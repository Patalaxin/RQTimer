/**
 * Роль пользователя. Дублирует Prisma-энум Role по значениям и живёт отдельно,
 * чтобы приложение не тянуло роль из mongoose-схемы: та осталась только ради
 * одноразовых скриптов переноса.
 */
export enum RolesTypes {
  Admin = 'Admin',
  User = 'User',
}
